{
  "name": "@ibexcairn/core-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types for the cost engine. Types only - no runtime dependencies, no I/O.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "clean": "tsc -b --clean"
  }
}
