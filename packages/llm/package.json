{
  "name": "@ibexcairn/llm",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-agnostic LLM interface (ILlmProvider) plus a Claude implementation and a skill loader. Keeps the engine off any single vendor or the VS Code Copilot API.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "clean": "tsc -b --clean"
  }
}
