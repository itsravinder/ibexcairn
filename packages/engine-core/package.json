{
  "name": "@ibexcairn/engine-core",
  "version": "0.0.0",
  "private": true,
  "description": "Headless fork of the upstream parsers + IR (S02). No vscode, no telemetry. Lifted from Azure/logicapps-migration-agent (MIT).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11"
  }
}
